spring:
  datasource:
    driverClassName: org.h2.Driver
    username: sa
    url: jdbc:h2:./devdb
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
